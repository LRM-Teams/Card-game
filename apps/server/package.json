{
  "name": "@card-game/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "斗地主后端：Node + Socket.IO（房间/匹配/对局状态机、服务端权威校验、真人不足自动补机器人）",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@card-game/rules": "workspace:*",
    "socket.io": "^4.8.1",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
